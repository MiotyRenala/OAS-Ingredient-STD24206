openapi: 3.0.0
info:
  title: TD OpenAPI Specification - Exercise 1
  version: 1.0.0
servers:
  - url: 'http://localhost:8080'

paths:
  /ping:
    get:
      responses:
        200:
          description: pong
          content:
            text/plain:
              schema:
                type: string
                example: pong
